# Pinned dependency set for the Koopman-assisted SINDy/PDE-FIND benchmarks.
#
# Provenance: these versions were resolved and import-tested at packaging time
# (every benchmark module imports cleanly under this set, including
# `from pydmd import BOPDMD`). They are NOT guaranteed to be the exact versions
# used to generate the published results/. For bit-exact reproduction of the
# paper's numbers, replace the pins below with the `pip freeze` output from the
# environment that produced results/.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
matplotlib==3.10.8
scikit-learn==1.8.0
pydmd==2025.8.1
kneed==0.8.6
SciencePlots==2.2.2

tabulate==0.9.0
